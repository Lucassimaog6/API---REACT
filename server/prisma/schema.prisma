generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:database.db"
}

model Language {
  id Int @id @default(autoincrement())
  name String
  description String
  stack String
  icon String
  usage Int
  desired Int
  loved Int
  category String
}